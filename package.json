{
  "name": "boron-context",
  "version": "0.7.8",
  "description": "Open-source intention and context layer for agentic work",
  "type": "module",
  "main": "./dist/cli.js",
  "bin": {
    "boron-context": "./dist/cli.js"
  },
  "files": [
    "dist",
    "eval",
    "migrations",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "npm run plugin:check && npm run typecheck && npm test && npm run eval:continuity && npm run eval:production-shaped",
    "dev": "tsx src/cli.ts serve",
    "eval:continuity": "npm run build --silent && node dist/eval/continuity-eval.js",
    "eval:production-shaped": "npm run build --silent && node dist/eval/production-shaped-eval.js",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "plugin:check": "node scripts/verify-plugin-cache-key.mjs",
    "start": "node dist/cli.js serve",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:postgres": "vitest run tests/postgres-integration.test.ts",
    "test:watch": "vitest",
    "db:migrate": "tsx src/cli.ts migrate",
    "service:print": "tsx src/cli.ts print-launchd",
    "service:install": "tsx src/cli.ts install-launchd"
  },
  "keywords": [
    "agents",
    "context",
    "intention",
    "ontology",
    "mcp",
    "postgresql"
  ],
  "author": "Boron Context contributors",
  "license": "MIT",
  "homepage": "https://github.com/kforris/boron-context",
  "repository": {
    "type": "git",
    "url": "https://github.com/kforris/boron-context.git"
  },
  "bugs": {
    "url": "https://github.com/kforris/boron-context/issues"
  },
  "engines": {
    "node": ">=20.19.0"
  },
  "dependencies": {
    "pg": "^8.22.0",
    "three": "^0.185.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^22.19.15",
    "@types/pg": "^8.20.0",
    "prettier": "^3.8.1",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.2"
  }
}
